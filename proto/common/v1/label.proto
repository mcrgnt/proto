syntax = "proto3";
package common.v1;

import "buf/validate/validate.proto";

message Label {
  string value = 1 [(buf.validate.field).string = {
    pattern: "^[a-z][a-z0-9_]{1,31}$"
  }];
}
