syntax = "proto3";

package common.v1;

import "buf/validate/validate.proto";
import "options/v1/options.proto";

message Port {
  uint32 value = 1 [
    (buf.validate.field).uint32 = {gte: 1, lte: 65535},
    (options.v1.usage) = "Порт сервера: 1-65535"
  ];
}
